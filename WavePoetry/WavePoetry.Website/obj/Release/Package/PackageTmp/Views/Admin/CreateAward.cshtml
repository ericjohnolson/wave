@model WavePoetry.Model.Category

@{
    ViewBag.Title = "Create Award Category";
}

<h2>Create Award Category</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend></legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>
        
        <div class="form-actions">
            <div class=" pull-right">
                <a href="/Admin/ListAwardCats/" class="btn">Cancel</a>
                <button class="btn btn-primary" name="cmdSave" type="submit">Save</button>
            </div>
        </div>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
